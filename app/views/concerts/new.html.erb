<div class="container text-center">
  <h1 class="text-center mt-5 mb-2">New Concert</h1>

  <%= render 'form', concert: @concert %>

  <%= link_to 'Back', band_concerts_path(@band), class: 'btn m-5 text-light' %>
</div>